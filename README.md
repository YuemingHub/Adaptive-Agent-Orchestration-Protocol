# Adaptive Agent Orchestration Protocol (AAOP)

> A host-agnostic protocol for turning a user goal into the smallest sufficient agent system: project understanding → capability planning → agent/skill/tool composition → execution → verification → replanning.

This repository is being initialized as an executable protocol package rather than a single giant prompt. See `AGENTS.md` and `.aaop/ORCHESTRATOR.md` after initialization.
